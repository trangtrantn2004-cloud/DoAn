BẬT XAMPP LÊN TRƯỚC VÀ START APACHE CÙNG VỚI MYSQL
Bước 1: Chạy backend trước.
cd backend
npm run dev
Bước 2: Mở 1 terminal khác để chạy
cd frontend
npm run dev

Bước 3: Mở trình duyệt vào localhost:5173

Về chatbot em sử dụng API của Google để gọi tới LLM (Mô hình ngôn ngữ lớn Gemini). và sử dụng kĩ RAG để tạo ngữ cảnh cho AI dựa trên dữ liệu thực tế của chủ doanh nghiệp( cửa hàng).

Em xây dựng ứng module hóa nhằm mục đích dễ dàng chỉnh sửa và nâng cấp hệ thống. 
